diff --git a/Makefile b/Makefile
index c04ddd0..3599f3e 100644
--- a/Makefile
+++ b/Makefile
@@ -1,6 +1,6 @@
 LDLIBS   ?= -lpulse -lboost_program_options
 CXXFLAGS ?= -std=c++11 -Wall -Werror -Wextra -pedantic
-PREFIX   ?= /usr/local
+PREFIX   ?= ~/.local
 RM       ?= rm -f
 
 target   := pamixer
@@ -28,6 +28,6 @@ distclean: clean
 install: pamixer
 	install -d $(DESTDIR)$(PREFIX)/bin/
 	install $(target) $(DESTDIR)$(PREFIX)/bin/
-	install -d $(DESTDIR)$(PREFIX)/man/man1/
-	install $(manpage) $(DESTDIR)$(PREFIX)/man/man1/
-	gzip $(DESTDIR)$(PREFIX)/man/man1/$(manpage)
+	install -d $(DESTDIR)$(PREFIX)/share/man/man1/
+	install $(manpage) $(DESTDIR)$(PREFIX)/share/man/man1/
+	gzip $(DESTDIR)$(PREFIX)/share/man/man1/$(manpage)
